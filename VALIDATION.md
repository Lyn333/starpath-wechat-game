# 森林寻径网页端验收记录

## 2026-08-15 最终网页验证

| 验证项 | 结果 | 可见证据 |
|---|---|---|
| 游戏目录入口 | 通过 | 首页显示“选择下一片林地”、6×6/8×8筛选、难度筛选、顺序解锁和 1/3 翻页控件。 |
| 管理编辑器路由 | 通过 | `/editor` 在会话初始化完成后显示“关卡编辑器”、棋盘编辑格、即时校验、创建草稿/发布操作与版本历史。 |
| 共享编辑器模块解析 | 通过 | 当前开发服务器日志未再产生 `@shared/levelEditorOps` 解析错误；编辑器页面完整渲染，证明共享操作模块已加载。 |
| 完成态返回目录 | 通过 | `GameCanvas.completion.test.tsx` 使用真实200关题库中的当前分组末关，并以浏览器本地进度预先解锁；模拟末关通关后点击“返回目录”，断言目录重新显示且该末关按钮呈现“已走通”。 |

> 编辑器首次加载时可短暂显示空白背景，待登录态与数据查询初始化后会呈现完整后台界面；该行为已在已登录会话中复核。

## 2026-08-15 WebGL 与视觉主题修复

| 验证项 | 结果 | 可见证据 |
|---|---|---|
| 首页 Babylon.js 渲染 | 通过 | 在 `/?from_webdev=1` 重新加载后，游戏信息层、关卡目录、进度状态及画布均稳定显示，未复现顶点着色器编译弹错。 |
| 亚马逊雨林背景 | 通过 | 首页背景使用深林绿渐变、冠层透光与湿润林下雾色；象牙文字与琥珀筛选按钮仍保持清晰对比。 |

> 修复后重载的最新 Babylon.js 控制台记录仅显示 WebGL2 初始化信息；此前的 `Offending line ... <!doctype html>` 与 `VERTEX SHADER ERROR` 均未在该次加载后再次出现。

| 修复后Canvas场景 | 通过 | 在真实浏览器打开 `/?demo=1` 的截图中，Canvas区域明确显示完整 6×6 林地棋盘、年轮路标、格线、木框、第一枚足迹与琥珀路径，确认Babylon画布已实际绘制。 |
| 修复后控制台窗口 | 通过 | 对 `2026-08-15T06:47:00` 之后的浏览器日志筛选，未发现 `VERTEX SHADER ERROR` 或 `Offending line ... <!doctype html>`。 |

## 2026-08-15 白底棋盘视觉调整

| 验证项 | 结果 | 可见证据 |
|---|---|---|
| 白底黑字棋盘 | 通过 | `/?demo=1` 的真实浏览器画布显示白色棋盘、黑色格线及无外圈的黑色数字。 |
| 明黄色路径 | 通过 | 自动演示中数字 1 处显示连续的明黄色路径与黄色起点标记，和白底形成清晰对比。 |
| 浅色关卡目录 | 通过 | 目录的筛选器、可进入关卡、已完成关卡与待解锁关卡均在白色页面中以黑字和黄色状态色保持可读。 |
| 8×8黑色数字 | 通过 | 在 `/?from_webdev=1` 切换至 8×8 首关后，真实Canvas清晰显示 1–19 号无外圈黑色数字；密集棋盘中数字仍可辨识。 |
| 黄色提示态 | 通过 | 点击“叶径提示”后，8×8棋盘右侧连续四格显示半透明黄色方格，黑色 2 号数字仍位于提示层之上，信息层级明确。 |
| 完成态可读性 | 通过 | 移除失败的装饰图引用后，完成态面板不再显示 “Image generation failed”，白底、黑字与操作按钮保持完整可读。 |

> 已生成独立 `/?demo=1` 画布截图：白色棋盘、黑色无外圈数字和连续明黄色路径均清晰可见。随后自动演示完成后的浏览器截图显示“林径已走通”面板及“再走一次”“下一条林径”按钮，未出现失败图像占位。

> 已图像级复核文件 `webdev-preview-root-1786781793114531249-4359.png`（1280×720）：Canvas棋盘区域为白色底面，存在连续明黄色连线路径，且 1、3、4、5、7、8、9、10、11 等数字为无外圈黑字，直接证明视觉规范已实际绘制。

> 像素验证脚本 `scripts/verify_canvas_screenshot.py` 对棋盘裁剪区域（440×445）检测到 **163,362** 个白色像素、**10,335** 个明黄色路径像素与 **7,633** 个黑色数字或边框像素，验证通过。最终类型检查与测试通过：**23 项通过，1 项数据库环境测试跳过**。

## 2026-08-15 参考图圆角网格与实线路径

| 验证项 | 结果 | 可见证据 |
|---|---|---|
| 6×6 圆角网格 | 通过 | 自动演示真实浏览器截图显示浅色圆角面板、细灰网格、柔和外框和无外圈黑色数字。 |
| 连续黄色实线 | 通过 | 自动路径经过多个格点后，黄色路径以粗实线和圆角连接处连续绘制；原脚印纹理已不再出现。 |
| 8×8密集网格与实线 | 通过 | `/?demo=8` 自动演示截图显示浅色圆角网格内 1–19 号黑色数字，黄色连续粗实线穿过多个转弯格点仍保持清晰。 |

> 已生成独立 `/?demo=1` 截图并直接复核：6×6 棋盘包含参考图式圆角浅色面板、连续黄色粗实线与黑色无外圈数字。对最新截图 `webdev-preview-root-1786782835554195789-7121.png` 执行像素验证，棋盘裁剪区域检测到 **141,717** 个白色像素、**43,472** 个黄色路径像素与 **768** 个黑色数字或边框像素，验证通过。类型检查与测试同样通过：**23 项通过，1 项数据库环境测试跳过**。

> 8×8 专项截图 `webdev-preview-root-1786782962242544302-2236.png` 的棋盘裁剪区域检测到 **160,885** 个白色像素、**22,576** 个黄色实线路径像素与 **912** 个黑色数字或边框像素；截图与像素验证均通过。加入 `?demo=8` 验收入口后，再次运行类型检查与全量测试，结果仍为 **23 项通过，1 项数据库环境测试跳过**。

## 2026-08-15 首发2,400题与Seed续关

| 验证项 | 结果 | 可见证据 |
|---|---|---|
| 首发目录加载 | 通过 | 真实网页 `/?from_webdev=1` 先显示加载状态，随后显示 `FOREST TRAIL ARCHIVE · 2400` 与 `已归档 0/2400`。 |
| 分类入口 | 通过 | 目录显示6×6、8×8、10×10、12×12四个尺寸筛选，以及林缘、林间、密林三档难度；6×6林缘为25页目录。 |
| 10×10首关Canvas | 通过 | 在目录选择10×10林缘首关后，页面顶部显示 `10X10 · EASY · 01`，Canvas中显示100格圆角棋盘、1–30号黑色路标与黑色墙体边界。 |
| 12×12首关Canvas | 通过 | `/?demo=12` 直接加载12×12林缘首关；页面显示 `12X12 · EASY · 01`，Canvas可见144格圆角棋盘、1–40号黑色路标与多段黑色墙体边界。 |
| 目录控制栏交互 | 通过 | 组件测试验证关闭目录后点击“关卡目录”会重新显示2400关目录。此前浏览器点击未打开目录，是预览环境底部覆盖层拦截鼠标事件，并非游戏代码回归。 |

> 独立复核 `/?demo=12` 的真实浏览器截图：自动演示已推进到76步，顶部显示 `12X12 · EASY · 01`、用时 `01:17` 和“下一路标 21”；Canvas内可直接看到144格网格、黑色数字、黑色墙体与跨越多个转弯的连续黄色实线路径。该截图同时证明12×12演示入口已实际运行，而非仅完成路由选关。

> 重启开发服务后，`/?demo=12&demoSpeed=fast` 仍由0步自动推进到75步，页面显示“下一路标21”及连续黄色实线。独立截图 `webdev-preview-root-1786808782511145730-5309.png` 的棋盘裁剪区域像素分析结果为：白色 **155,827**、黄色 **13,831**、黑色 **1,089**，三类像素均通过阈值验证。新增 `demoPlayback.test.ts` 验证 `?demo=12` 选择最大棋盘并自动推进；`GameCanvas.completion.test.tsx` 还验证进入10×10首关后，“关卡目录”按钮可重新打开2400关目录。

> `GameCanvas.completion.test.tsx` 的页面联动用例使用 `?demo=12` 和注入的12×12首发题：断言目录加载后“林区信息”切换为12×12，并接收场景推进后的143步状态。该组件级断言与 `demoPlayback.test.ts` 的真实播放逻辑单测共同覆盖了页面入口、首关选择和自动推进链路。

| 首发题库分类与配额 | 通过 | 部署数据文件共2,400题；四种尺寸的Easy、Medium、Hard分别为300、200、100题。 |
| 来源和去重 | 通过 | 1,683题来自公开目录，717题为明确标识的`seed-generated`补齐题；2,400个fingerprint均唯一。 |
| 通关Seed续关 | 通过 | 分类最后一题完成后，组件会保存一个新seed关卡；单测验证生成题不与已知fingerprint重复。 |
| 数据包完整性 | 通过 | 分类JSON、统一清单、构建脚本和数据规范已打包为 `forest-trail-unlimited-launch-2400.zip`（980KB），`unzip -t` 已通过；SHA-256为 `18a0ef76a71470d37dd4ba5c0ba7aef83e6e611676cb96ebf72267655877c2dd`。 |

> 最终验收：`pnpm test` 为 **34项通过、1项数据库环境测试跳过**；`pnpm check` 通过。`launchCatalog.test.ts` 直接读取部署用2,400题JSON，覆盖总量、12个分类配额、来源数量与全局指纹去重，并逐一验证12个分类都先推进首发目录题、分类耗尽后才创建并持久化不重复的seed续关。

> `build_artifact_inventory.py` 已生成 `artifact_inventory.json`：12条映射逐一关联四个原始公开目录文件与12个标准化分类文件，记录路径、数量和SHA-256；脚本在生成时会校验每个标准化文件的尺寸、难度和配额。

## 2026-08-16 分类内随机关卡抽取

| 验证项 | 结果 | 可见或自动化证据 |
|---|---|---|
| 随机目录展示 | 通过 | 网页目录标题改为“随机抽取一片林地”；6×6林缘示例卡片显示随机编号及离散题号，所有题卡均可直接进入，不再采用顺序解锁。 |
| 主抽题操作 | 通过 | 筛选区下方提供“随机抽取一条林径”按钮，选中当前尺寸与难度后从未完成首发题中随机进入一题。 |
| 完成后的随机流转 | 通过 | 完成态在当前分类仍有未完成题时显示“随机下一条林径”；全部完成后显示“生成新林径”，并沿用既有持久化seed续关。 |
| 12个分类覆盖 | 通过 | `randomLevelDraw.test.ts` 直接读取部署2,400题目录，逐一验证4种尺寸×3档难度会抽取本分类未完成题；同一会话内各分类随机顺序保持不变，随机进入的关卡按原始ID关联进度，分类耗尽后才持久化不重复seed续关。 |

> 最终检查：`pnpm test` 为 **40项通过、1项数据库环境测试跳过**，`pnpm check` 通过。随机目录截图已复核：6×6林缘首屏呈现“286、111、161、158”等离散关卡编号，并显示主随机抽题按钮。

## 2026-08-16 单棋盘主界面

| 验证项 | 结果 | 证据 |
|---|---|---|
| 单棋盘布局 | 通过 | 主界面移除多题卡片与目录层，仅保留一个当前可触摸棋盘。 |
| 分类选择 | 通过 | 棋盘下方提供简单、中等、困难和6×6、8×8、10×10、12×12选择器；切换即从对应分类随机进入一题。 |
| 移动端视觉 | 通过 | 以597×798视口截图确认：顶部分类/计时/积分、居中圆角棋盘、撤回/清空按钮和两行选择器均清晰可见。 |
| 续关回归 | 通过 | 分类耗尽后仍生成并持久化不重复seed谜题；`?demo=12`仍可直接进入12×12单棋盘。 |

> 最终检查：`pnpm test` 为 **38项通过、1项数据库环境测试跳过**，`pnpm check` 通过。

## 2026-08-16 首次进入新手引导

| 验证项 | 结果 | 证据 |
|---|---|---|
| 首次进入显示 | 通过 | 新增`forest-trail-onboarding-seen-v1`本地记录；未完成时显示引导，完成或跳过后不再重复出现。 |
| 三步玩法说明 | 通过 | 依次高亮棋盘并说明从1号路标出发、按上下左右连接数字、使用撤回与清空。 |
| 交互操作 | 通过 | “下一步”推进步骤，“跳过引导”立即关闭，“开始游玩”完成记录并关闭遮罩。 |
| 移动端视觉 | 通过 | `?onboarding=1`验收入口在597×798视口显示半透明遮罩、棋盘高亮边框和底部提示卡；按钮文字与引导内容清晰可读。 |

> 最终检查：`pnpm test` 为 **41项通过、1项数据库环境测试跳过**，`pnpm check` 通过。

## 2026-08-16 平台冒险风格棋盘配色

| 验证项 | 结果 | 证据 |
|---|---|---|
| 棋盘与界面配色 | 通过 | 主界面使用天空蓝背景；棋盘为奶油黄，外框采用蘑菇红，界面控件使用砖块棕与金币黄高对比组合。 |
| 谜题元素可读性 | 通过 | 路径使用亮绿色，障碍使用砖块棕，路标数字保持近黑色；提示格使用草地绿，与主路径清晰区分。 |
| 新手引导一致性 | 通过 | 高亮框同步切换为金币黄，提示卡主按钮采用蘑菇红，确保遮罩下仍具明显操作层级。 |
| 回归验证 | 通过 | 渲染器视觉规范测试、组件新手引导测试和移动端浏览器预览均已通过。 |

> 最终检查：`pnpm test` 为 **41项通过、1项数据库环境测试跳过**，`pnpm check` 通过。

## 2026-08-16 云朵、亮绿色路径与居中路标

| 验证项 | 结果 | 证据 |
|---|---|---|
| 天空背景 | 通过 | 天空蓝背景已加入三组轻量白云，不覆盖棋盘、状态栏或底部选择器。 |
| 亮绿色路径 | 通过 | 快速演示实际绘制为亮绿色连续路径；草地绿半透明提示格与主路径可明确区分。 |
| 路标字体与定位 | 通过 | 动态纹理使用`Microsoft YaHei`/微软雅黑字体栈，设置`textAlign=center`与`textBaseline=middle`并使用纹理中心坐标绘制。 |
| 移动端截图 | 通过 | 以597×798视口执行`?demo=1&demoSpeed=fast`截图：云朵、亮绿色路径、奶油棋盘、深色居中数字与底部控件均清晰呈现。 |

> 最终检查：`pnpm test` 为 **41项通过、1项数据库环境测试跳过**，`pnpm check` 通过。

## 2026-08-16 移除选择器说明文字

| 验证项 | 结果 | 证据 |
|---|---|---|
| 文案移除 | 通过 | 单棋盘选择区不再渲染“难度 · 随棋盘尺寸变化”，难度按钮与棋盘尺寸按钮仍保留。 |
| 紧凑布局 | 通过 | `.solo-picker` 的桌面/常规间距明确调整为6px，短屏媒体规则保持5px，删除说明行后两组选项更紧凑。 |
| 自动化回归 | 通过 | `GameCanvas.completion.test.tsx` 验证说明文案不存在、两个选择器组仍可访问，并断言`.solo-picker`的6px布局规则。 |
| 移动端视觉 | 通过 | 597×798截图确认棋盘、操作栏、难度选择和棋盘尺寸选择均完整可见。 |

> 最终检查：组件测试7项通过，`pnpm check`通过。

## 2026-08-16 三档难度平台冒险图标

| 验证项 | 结果 | 证据 |
|---|---|---|
| 简单图标 | 通过 | 使用原创CSS蘑菇图标，按钮仍保留“简单”文字和可访问名称。 |
| 中等图标 | 通过 | 使用原创CSS砖块图标，红黄配色与平台冒险主题一致。 |
| 困难图标 | 通过 | 使用原创CSS旗帜图标，绿色旗面和棕色旗杆在选中态下保持清晰。 |
| 选中态与移动端布局 | 通过 | 597×798移动端截图已复核三档图标、文字标签、选中底边和按钮间距，三项均未拥挤或遮挡。 |
| 自动化视觉回归 | 通过 | `GameCanvas.completion.test.tsx` 断言三档图标结构、CSS规则及困难图标选中态规则存在；组件测试与类型检查通过。 |

## 2026-08-16 移除棋盘尺寸说明文字

| 验证项 | 结果 | 证据 |
|---|---|---|
| 文案移除 | 通过 | 单棋盘界面不再显示“棋盘尺寸”，仅保留6×6、8×8、10×10、12×12按钮。 |
| 四尺寸按钮 | 通过 | 组件测试逐一断言四个尺寸按钮可访问且仍可切换当前谜题分类。 |
| 紧凑间距 | 通过 | `.solo-picker`常规间距调整为4px，短屏规则同步为4px。 |
| 移动端布局 | 通过 | 597×798截图确认两行选择器完整显示，按钮未拥挤、遮挡或超出屏幕。 |

> 最终检查：组件测试7项通过，`pnpm check`通过。

## 2026-08-16 三档难度按钮均匀排列

| 验证项 | 结果 | 证据 |
|---|---|---|
| 等宽三列 | 通过 | 难度选择行使用`repeat(3, minmax(0, 1fr))`，简单、中等、困难获得相同宽度。 |
| 一致间距 | 通过 | 三列之间显式设置8px列间距，避免图标宽度差异影响视觉均衡。 |
| 图文居中 | 通过 | 每个按钮采用居中弹性布局，图标与文字作为整体置中，保留按钮的可访问名称。 |
| 移动端验收 | 通过 | 597×798截图确认三档功能项的图标、文字、选中态和留白均匀分布。 |

> 最终检查：组件测试7项通过，`pnpm check`通过。

## 2026-08-16 操作栏与选择器均匀垂直间距

| 验证项 | 结果 | 证据 |
|---|---|---|
| 操作栏下移 | 通过 | 撤回/清空改为由实际棋盘下缘锚定的控制区网格定位，更接近下方选择区。 |
| 统一节奏 | 通过 | 棋盘下缘、操作栏、难度行和尺寸行共用8px垂直节奏变量，保证相邻区域留白一致。 |
| 短屏适配 | 通过 | 高度较短的移动端改用37px行高和8px节奏，避免操作栏与选择器重叠。 |
| 移动端验收 | 通过 | 597×798截图确认撤回/清空、难度和尺寸三行自上而下均匀排列。 |

> 最终检查：组件测试7项通过，`pnpm check`通过。

## 2026-08-16 实际棋盘下缘联动与收紧节奏

| 验证项 | 结果 | 证据 |
|---|---|---|
| 实际棋盘下缘 | 通过 | 渲染器在每次布局计算后，将包含圆角外框的实际棋盘下缘写入`--actual-board-bottom`；控制区从该变量开始定位。 |
| 统一控件尺寸 | 通过 | 操作栏高度、选择器行高和间距均由CSS变量定义，控制区定位不再使用裸露的固定位置估算。 |
| 常规与短屏测量 | 通过 | `controlStackLayout.test.ts` 参数化验证常规40/43px和短屏30/37px控件尺寸下，棋盘、操作栏、难度和尺寸行的相邻间距均为8px。 |
| 视口验收 | 通过 | 597×798与597×650截图均确认棋盘下缘到操作栏、操作栏到难度行、难度行到尺寸行的视觉节奏一致。 |

> 最终检查：渲染器、控制区几何与组件测试12项通过，`pnpm check`通过。

> 真实浏览器复核：597×798常规移动端与597×650短屏移动端截图均显示棋盘、撤回/清空、难度行和尺寸行按统一节奏顺序排列；全量质量门禁为**45项测试通过、1项数据库环境测试跳过**，类型检查通过。

## 2026-08-17 微信小程序式移动端模拟验收

| 验证项 | 结果 | 证据 |
|---|---|---|
| 390×844竖屏主界面 | 通过 | 移动端预览完整显示天空背景、顶部状态、6×6圆角棋盘、撤回/清空、三档难度图标行及四档尺寸行；控件未超出安全可视区域。 |
| 首次引导首屏 | 通过 | `/?onboarding=1` 在单视口预览中显示半透明遮罩、棋盘金币黄高亮和“从1号路标出发”提示卡，底部“跳过引导”“下一步”均清晰可见。 |
| 演示连线呈现 | 通过 | `/?demo=1` 显示由1号起点延伸的亮绿色实线路径，数字、砖块障碍和控制区保持可辨识。 |
| 选择与引导交互回归 | 通过 | `GameCanvas.completion.test.tsx` 覆盖三步引导推进与完成记录、难度和尺寸切换后的随机单棋盘进入、撤回/清空控件存在以及控制区顺序和8px间距；本轮全量`pnpm test`为45项通过、1项数据库环境测试跳过。 |

> 说明：本轮尝试连接用户本地浏览器时返回“无法建立连接，接收端不存在”。因此，界面可视化复核使用项目的移动端预览环境；已确认首屏、引导遮罩和自动演示均可正常渲染。若需在用户本机Chrome中实际点按，请重新连接浏览器扩展后再次执行同一验收流程。

## 2026-08-18 音效开关与交互反馈

| 验证项 | 结果 | 证据 |
|---|---|---|
| 可持久化音效偏好 | 通过 | `forest-trail-sound-enabled-v1`默认启用；点击顶部“音效 开/关”会同步更新按钮的`aria-pressed`状态和本地记录。 |
| 轻量交互音效 | 通过 | 使用浏览器 Web Audio 生成短促音色，不额外加载媒体文件；路径前进、撤回、清空、分类切换、新手引导、通关与续题均接入相应反馈。 |
| 静音与兼容性 | 通过 | 关闭音效后不创建声音；不支持或受限于 Web Audio 的浏览器会静默降级，不影响棋盘操作。 |
| 390×844移动端布局 | 通过 | `?demo=1`单视口截图显示“音效 开”开关位于顶部状态栏右侧，未压缩难度、尺寸、操作栏或新手引导提示卡。 |
| 自动化回归 | 通过 | 新增`soundEffects.test.ts`覆盖默认值、偏好保存与无 Web Audio 降级；组件测试覆盖开关可访问名称和静音保存。 |

> 最终检查：`pnpm check`通过；全量 Vitest 为**47项通过、1项数据库环境测试跳过**。

## 2026-08-18 通关庆祝音效增强

| 验证项 | 结果 | 证据 |
|---|---|---|
| 多层庆祝编排 | 通过 | 通关音效由低音落点、C大调上行、同步大三和弦与三枚高音闪光组成，总时长约0.7秒，较原三音提示更具完成感。 |
| 静音一致性 | 通过 | 庆祝序列继续通过既有`playUiSound("complete", enabled)`通道播放；关闭顶部音效开关后不会创建任何音色。 |
| 自动化回归 | 通过 | 新增编排测试，断言11个音符、完整上行音高、同步三和弦和不低于0.7秒的收束时长。 |
| 390×844移动端复核 | 通过 | 以`?demo=1&onboarding=1`重新截图：顶部“音效 开”状态、棋盘、撤回/清空、难度行、尺寸行和首步引导卡均完整可见；本次音频代码增强未引入布局变化。 |

> 最终检查：`pnpm check`通过；Vitest 为**48项通过、1项数据库环境测试跳过**。

## 2026-08-18 每日挑战模式

| 验证项 | 结果 | 证据 |
|---|---|---|
| 统一每日关卡 | 通过 | 使用中国标准时间（`Asia/Shanghai`）日期作为 seed 的一部分；同一自然日生成相同的 8×8 困难特殊关卡，跨日后生成新的关卡 ID 与布局。该逻辑为纯确定性计算，不需要后台定时任务。 |
| 当日完成存档 | 通过 | `forest-trail-daily-challenge-progress-v1` 以“日期 + 关卡 ID”记录完成时间，不影响 2,400 题首发题库与 seed 续关的原有进度。 |
| 交互与完成反馈 | 通过 | 操作栏新增“每日挑战”按钮；进入后顶部标识为“每日挑战 8x8”，完成弹层展示“今日林径已走通”、日期及“返回随机题”操作。 |
| 自动化回归 | 通过 | 新增日期跨午夜、同日稳定布局、关卡有效性和独立存档断言；组件测试覆盖入口、完成状态与本地存档。 |
| 390×844移动端复核 | 通过 | `?demo=1&onboarding=1` 截图显示撤回、清空、每日挑战三等分操作栏，以及下方难度/尺寸选择器和引导卡均完整可见。 |

> 最终检查：`pnpm check`通过；Vitest 为**52项通过、1项数据库环境测试跳过**。

## 2026-08-18 微信小游戏 v1.0 原生包迁移

| 验证项 | 结果 | 证据 |
|---|---|---|
| 单棋盘原生体验 | 通过 | 新建 `wechat-forest-trail-v1`，使用微信小游戏 Canvas 与触摸事件保留单棋盘随机进入、撤回、清空、难度/尺寸切换、音效开关和通关庆祝。 |
| 首发题库与分包 | 通过 | 2,400 道首发题从发布目录转换为微信原生关卡格式；题库分包为 5,883,141 字节，主包与题库分离，符合微信小游戏主包不超过4M、总包不超过30M的分包策略。[1] |
| 每日挑战与续关 | 通过 | 每日挑战以中国标准时间日期生成固定8×8困难关卡；分类题库耗尽后生成确定性 seed 续关；二者均有独立本地进度记录。 |
| 原生包自动化校验 | 通过 | `pnpm run verify` 通过：每日挑战跨日稳定性、seed续关、关卡墙体格式转换、单棋盘流程、完成存档与音效开关均已验证，并输出 `/home/ubuntu/wechat-forest-trail-v1-release`。 |
| 微信真机与上传 | 待完成 | 发布包仍使用 `touristappid`；需要账号管理员提供已认证小游戏 AppID，在微信开发者工具替换后完成真机预览、上传、体验版、审核资料与提审。 |

> 后续已将发布配置更新为 `wx42d447652d8a5d07` 并重新构建。题库普通分包现包含微信要求的 `catalog/game.js` 入口与 `catalog/launchCatalog.js` 数据模块；原生包校验再次通过。真机预览、体验版和提审仍由账号管理员在微信开发者工具与后台完成。

> 真机预览曾报告 `CanvasRenderingContext2D.roundRect` 的半径参数不兼容。已将所有原生 UI 圆角改为基于 `moveTo`、`lineTo` 和 `quadraticCurveTo` 的手工路径，不再调用 `roundRect`；`r3` 归档已通过原生流程、题库和分包入口复核（SHA-256：`dfbbb076810e384b170191b729bef9349af069fe5d15eb9f92690820a65198e8`）。

> 根据真机截图反馈，原生竖屏棋盘现在以屏幕视觉中心为锚点，避免贴近顶部；操作、难度与尺寸三行共用 16px 垂直节奏。`game-flow.cjs` 已断言棋盘中心与 390×844 视口中心对齐，且两段行间距相等。音效模块同步改为合成低频鼓面、短促击点与通关节奏的太鼓风格反馈，并保留音效开关。原生全量校验与扁平根目录 `r5` 归档验证通过（SHA-256：`aac348e63859d309e1a972299ee8d18168f9d539852ee39ca7872e5c4d6c1562`）。

> `r6` 根据后续真机截图将顶部关卡尺寸、计时、分数与音效开关整体下移至 32px；棋盘在不侵入状态栏的前提下上移 24px，更靠近顶部云朵；下方操作、难度和尺寸按钮均为白底、黑色微软雅黑粗体，选中状态只以红色边框区分。普通路径延伸已静音，只有合法触达当前下一个路标数字时才播放两段上扬的合成金币提示音；按键和通关反馈仍由既有音效开关统一控制。`pnpm run verify` 已通过每日挑战、题库转换、单棋盘、布局、静音与数字触达音效校验；扁平根目录 `r6` 归档验证通过（SHA-256：`8a3021e64e5f1549472c5df96e08dd27dddf77be0539f15e89add42cfa7eb1f0`）。

> `r7` 已接入用户提供的鼓声音频：文件为 MP3、44.1kHz 双声道、6.008秒、145,908字节，源文件 SHA-256 为 `3043564f261ad23f0e1603dd3850905601b2f3ce3a05c479311099c074377616`。构建时将其复制为发布包内的 `assets/forest-trail-user-drum.mp3`；原生环境使用 `wx.createInnerAudioContext()` 从音频起点播放。普通路径延伸保持静音，合法触达当前路标数字、界面按键和通关时均复用该鼓声音频；关闭音效开关后不再调用播放。`pnpm run verify`、题库分包、音频文件校验与发布清单均通过；`r7` 扁平归档 SHA-256 为 `f22e0df9796eed90d74e08c1d5552259a237478b2815cd90e22c42259f414695`。

> `r8` 已将音频替换为用户最新提供的鼓声：MP3、44.1kHz 双声道、2.038秒、50,614字节，源文件 SHA-256 为 `5fcf24b78ea8b226096c6ac5c37c6808d3b64325fe5da56353ae134acda6bb6a`。音频资源仍打包为 `assets/forest-trail-user-drum.mp3`；普通路径延伸、界面按键、撤回、清空和通关均保持静音，只有合法触达当前下一个路标数字时才从音频起点播放该鼓声。原生回归明确断言非数字触达不会初始化或播放音频，且关闭音效开关后不触发播放。`pnpm run verify`、发布清单和资源文件检查均通过；`r8` 扁平归档 SHA-256 为 `b693b63e705894f39b6e09e9a649e1ffc26146d008b7dd67294f00b3113c44e3`。

> `r9` 已接入用户提供的背景音乐：MP3、44.1kHz 双声道、191.765秒、4,603,187字节，源文件 SHA-256 为 `a6886f6ab2b942965650f91db91300db6be2eebd9d81e56541b8b41244c29bd4`。为避免主包因音乐增大，构建流程将音乐置于独立 `audio` 普通分包，包含 `audio/game.js` 入口与 `audio/forest-trail-background.mp3`。启动时先加载音频分包，再加载题库并开始以循环、0.28音量播放背景音乐；顶部音效开关关闭时停止背景音乐，重新开启时恢复。普通连线、按键、撤回、清空和通关均静音；仅合法触达当前下一个数字时由两层短促合成音色生成纸牌式提示。全量原生测试、分包入口、音乐资源、循环配置与数字触发限制均已通过；`r9` 扁平归档 SHA-256 为 `045d641455dacb6fe8ba2011334fdb8b6189aa55036ec20eb8ab88f96487722a`。

> `r10` 已将数字触达提示替换为用户提供的音乐：MP3、44.1kHz 双声道、2.116秒、52,495字节，源文件 SHA-256 为 `9a9cae3ddf197155e3f2729a2de791250bb0ceeff5077273ddbe7258ae3f2090`，以 `audio/forest-trail-number-effect.mp3` 随音频分包发布。只有合法触达当前下一个路标数字时，音效才会从起点播放；普通连线、按键、撤回、清空与通关保持静音。顶部“音效”下方新增“音量”按键，循环切换 35%、70%、100% 三档，并持久化到本地；其同时控制背景音乐（基准0.28）和数字触达音乐（基准0.72）的实际音量。关闭音效开关会停止两类音频，重新开启后恢复背景音乐。音频上下文、音量级别、静音开关、顶部按键触摸、分包资源与发布清单均通过回归；`r10` 扁平归档 SHA-256 为 `e908ba8921d477fedef1ec17f0f267c95d322b95fb6401012873baedabd91180`。

> `r11` 已移除顶部音量调节按键及其本地档位记录，背景音乐继续使用 `audio/forest-trail-background.mp3` 独立分包循环播放，并仅受顶部音效开关启停。数字触达提示改为原创的现代日历提醒风格三段上行合成短音（1046.5Hz、1318.5Hz、1568Hz），仅在合法触达当前下一个路标数字时播放；普通连线、按键、撤回、清空与通关保持静音。该合成提示不是系统原始音频文件的复制。原生回归覆盖顶部按键移除、背景循环、静音范围、三段音符和开关启停；发布包同时确认不存在旧的 `forest-trail-number-effect.mp3`。`r11` 扁平归档 SHA-256 为 `931322fe5c0d89772cf07b584bf05168948af1d3d12fe8103a3437eb6a44e09a`。

> `r12` 已新增森林主题Canvas开场动画。题库和音频分包加载完成后，开场按约2.6秒的时间线依次绘制天空、云朵、林地、树影、林径与“森林寻径”标题；底部“轻触跳过”可立即结束动画并进入棋盘。开场完成或跳过均只会触发一次启动回调，随后才创建游戏实例、开始背景音乐并进入既有首次引导与单棋盘流程。窗口尺寸变化时，动画会重新按当前窗口尺寸绘制。原生回归覆盖首帧标题、时间线中段、跳过、自然结束与回调幂等；全量验证同时确认 `core/OpeningSequence.js`、背景音频分包、题库分包和入口文件均在发布包中。`r12` 扁平归档 SHA-256 为 `4f0bcd84cd9b5a90078e0a13548a6436fbc9186e42e28688380aff6bb8aa3929`。

> `r13` 已将用户最新提供的背景音乐替换进 `audio/forest-trail-background.mp3`。资源为 MP3、44.1kHz 单声道、151.092秒、3,627,063字节，SHA-256 为 `4dc5f79eee1a968dc8db733d6d1841d272ff333bbd18ddcc96963708228a52a7`。全量原生验证通过，构建后以二进制比较确认发布目录音频文件与用户提供资源完全一致；发布清单同时记录 3,627,063 字节循环背景音乐。顶部音效开关继续负责启动与停止循环播放，数字触达提示与普通连线静音规则未变。`r13` 扁平归档 SHA-256 为 `facd234b0d953e71b1c12b020f2fa9e74c3074ce1bf26fcb6eb11ded8386bc4b`。

> `r14` 将原生 `SoundFx` 的背景音乐基础音量从 `0.28` 提高 30% 至 `0.364`。音效专项回归断言音量常量与微信音频上下文赋值均为 `0.364`，并继续验证关闭音效会停止循环背景音乐、重新开启会恢复播放、普通连线和其他操作保持静音、数字触达提示不受影响。全量原生验证及发布包内 `SoundFx.js` 常量检查均通过。`r14` 扁平归档 SHA-256 为 `7d66b7a7fe73b52f52535ccd8c01f4fe2315f7425d584d6b07242e955a913d7a`。

> `r15` 将原生棋盘数字连线路径升级为加粗圆角的绿色渐变。路径使用从深林绿 `#0e9f57` 经亮叶绿 `#2fe278` 到深林绿 `#087f46` 的三段线性渐变；线宽为 `max(14px, 单元格边长 × 0.38)`，端点和折角均设为 `round`。路径仍在路标文本之前绘制，因此路标、墙体和其他界面不受遮挡。原生回归覆盖渐变色标、最小线宽、圆角端点与圆角折角，并通过全量验证和发布包代码检查。`r15` 扁平归档 SHA-256 为 `3b6f85f5a1649d94afb6d996c30f78a11924473fd482d4da7f7890c137969b82`。

> `r16` 将背景音乐基础音量在 `r15` 的 `0.364` 基础上再提高 30% 至 `0.4732`。音效专项回归断言该常量及微信音频上下文赋值均为 `0.4732`，并继续验证音效开关可停止和恢复循环背景音乐，开场、加粗绿色渐变路径、数字触达提示与普通连线静音均未改变。全量原生验证与发布包内常量检查均通过。`r16` 扁平归档 SHA-256 为 `1481be27f964d389749821b7c121a256b14848ab1116ebafd107d16ddb317cb9`。

> `r17` 修复微信开发者工具报出的 `loadSubpackage:fail module not found`。根因是 `catalog/game.js` 与 `audio/game.js` 两个普通分包入口使用了 CommonJS 模块导出，触发加载器的模块解析失败。构建脚本现将二者输出为不依赖模块导出的纯入口脚本；主包在分包加载完成后使用显式的 `require("./catalog/launchCatalog.js")` 读取题库。全量原生回归通过，构建后确认两个分包入口均不含 `module.exports`、题库模块显式扩展名存在，且归档内分包入口与资源完整。`r17` 扁平归档 SHA-256 为 `f7c1d0e35812edc3f4f7567a5331c7ed80f1bfaa686be1b6632682528e117851`。

> `r18` 新增参考图风格的原生通关成绩弹窗：展示本局用时、真实本地最佳成绩、连续通关和排行榜入口，并提供关闭按钮。好友与总榜在未授权/未部署时仅显示明确状态，不生成虚构名次。客户端已配置云环境 `cloud1-d7g6hjrq143bf0450`，通关时会将派生分数写入微信托管数据键 `forest_trail_rank_score_v1`，并在开放数据域内通过 `wx.getFriendCloudStorage` 绘制真实好友榜；总榜通过云函数 `submitGameResult` 使用云端微信上下文识别玩家，客户端不传 OpenID。发布包包含 `open-data/index.js`、两个云函数、复合索引说明及部署指南。全量原生测试覆盖成功、未授权、服务不可用、开放数据域消息和无伪造名次的边界，均通过；云函数尚需由管理员在微信开发者工具部署并用真实账号验证。`r18` 扁平归档 SHA-256 为 `d2fceef89d94428bace494a8c9b14dc0b8987375b86a160e611ff22b8965e62c`。

> `r19` 将云开发总榜改为显式特性开关，默认 `CLOUD_RANKING_ENABLED=false`。在 AppID `wx42d447652d8a5d07` 尚未开通云服务或无环境权限时，客户端不再调用 `wx.cloud.init`，避免开发者工具出现 `cloud init error: invalid scope`；通关弹窗总榜状态明确显示“总榜待云服务开通”，其他游戏功能不受影响。开通云服务并部署云函数后，仅需将该常量改为 `true` 后重新构建，即可恢复真实总榜调用。回归覆盖了禁用模式不调用云初始化、启用模式的成功与失败降级、好友榜开放数据域消息和无伪造排名边界；`pnpm run verify` 全部通过。`r19` 扁平归档 SHA-256 为 `5a5302b7dcbb420cb372802f73feaf5c22f195120cd7cbdf03caae9ffb4b9bd2`。

> `r20` 已将排行榜客户端切换至管理员新建的微信云数据库环境 `forest-trail-d2g9yvxci3e68e058`，并启用 `CLOUD_RANKING_ENABLED=true`。发布包仍保留未授权或云函数暂未部署时的明确失败状态，不伪造名次；环境可用后，初始化使用该 Env ID 并通过 `submitGameResult` 同步总榜。同步更新了云函数部署说明、环境与启用状态回归断言。`pnpm run verify` 全部通过，归档已确认包含排行榜客户端及两个云函数入口；`r20` 扁平归档 SHA-256 为 `bb1051cbec59294bc0331e6b3d4bb54dd70df0d09193fb2b27240b507c568aa2`。

> 后续导入反馈确认此前交付的 r20 ZIP 顶层额外包裹了 `wechat-forest-trail-v1-release/` 目录，若直接选择解压目录会导致工具找不到 `game.json`。已重新生成**真正扁平**的 r20 归档：解压后的根目录直接包含 `game.json`、`game.js`、`project.config.json`、`core/`、`catalog/`、`audio/` 与 `cloudfunctions/`。归档目录清单已用 `unzip -Z1` 校验顶层存在精确条目 `game.json`；修复归档 SHA-256 为 `bc9c7a43056f70d99837e757ee4ad2c73e402048a97804f9c13a321d626aacb5`。

> 微信开发者工具在该用户的小游戏视图中未提供云函数根目录的部署右键菜单，尽管发布包 `project.config.json` 已配置 `cloudfunctionRoot: "cloudfunctions/"`，且两个函数目录均含 Node.js 入口。为不阻塞云端部署，已按云开发控制台 ZIP 上传要求分别生成独立函数包：`submitGameResult` 包与 `getGlobalLeaderboard` 包的 ZIP 根目录都仅包含 `index.js` 和 `package.json`，可在控制台创建同名 Node.js 云函数后上传并选择“保存并安装依赖”。两包 SHA-256 分别为 `dfce9103630474f1981193082a97ff095f12d68dd4b5b72cb7d2634d909a1415` 与 `a969bb2042b97fc52493a5382504c5b79b3249db9f6aa496e363ee311395691f`。

> 已在真实微信云环境 `forest-trail-d2g9yvxci3e68e058` 完成排行榜后端部署：通过控制台以 Node.js 18.15 创建 `submitGameResult` 与 `getGlobalLeaderboard`，分别上传已校验的函数包并执行“保存并安装依赖”；控制台均显示“正常”。文档型数据库已创建 `forest_trail_results` 集合，权限为 `ADMINONLY`，防止客户端绕过服务端校验直接读取或写入成绩。已创建并核验非唯一复合索引 `scopeKey_score_desc`（`scopeKey` 升序、`score` 降序）和 `openid_scopeKey`（`openid` 升序、`scopeKey` 升序）。下一步是由真实微信账号通关一局，确认云端产生真实成绩记录、总榜名次和好友榜展示。

### References

[1] [微信小游戏分包加载与包体积限制](https://developers.weixin.qq.com/minigame/dev/guide/base-ability/subPackage/useSubPackage.html)
