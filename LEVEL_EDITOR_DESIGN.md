# 森林寻径：关卡编辑器与后端管理设计

## 目标

关卡编辑器面向内容管理员，用于创建、校验、预览、发布、归档和回滚森林寻径的路径谜题。游戏客户端只读取**已发布版本**；编辑行为始终作用于草稿或新版本，避免修改中的内容直接影响玩家。

## 生命周期与权限

关卡以“逻辑关卡”和“不可变版本”两层管理。逻辑关卡拥有稳定 ID、标题、推荐难度与当前发布版本；每一次保存形成一个带序号的版本快照。草稿可反复编辑；发布会将通过完整性校验的版本标为当前版本；归档会使关卡不再出现在游戏关卡列表，但保留历史记录；回滚实际是把某个历史版本重新发布，而不是覆盖历史数据。

| 状态 | 允许操作 | 玩家可见性 | 备注 |
|---|---|---|---|
| `draft` | 编辑、校验、删除未发布版本 | 不可见 | 仅内容管理员可读写。 |
| `validated` | 预览、发布 | 不可见 | 已完成完整路径与路标顺序校验。 |
| `published` | 归档、创建新草稿、回滚 | 可见 | 每个逻辑关卡仅有一个当前发布版本。 |
| `archived` | 重新发布或永久删除（保留管理员操作） | 不可见 | 默认不出现在玩家列表。 |

第一版将后台读写接口保护为**已登录用户**；生产环境应在用户资料中增加 `role=admin` 后再将管理路由限制为管理员。公开游戏接口只返回 `published` 版本，且不返回 `solution` 字段，防止客户端直接读取答案。

## 数据模型

| 实体 | 核心字段 | 说明 |
|---|---|---|
| `levels` | `id`、`slug`、`title`、`size`、`difficulty`、`status`、`published_version_id` | 逻辑关卡与当前发布指针。 |
| `level_versions` | `id`、`level_id`、`version_number`、`snapshot`、`validation`、`created_by` | 不可变版本快照；`snapshot` 保存行列、路标、倒木和解法。 |
| `level_publications` | `id`、`level_id`、`version_id`、`action`、`created_by` | 发布、回滚和归档的审计记录。 |

`snapshot` 采用与微信小游戏核心一致的 JSON 结构：`rows`、`cols`、`waypoints`、`walls`、`solution`。墙体格式保留 `H_row_col` / `V_row_col`，因此编辑器、后端和小游戏端可以共享同一份关卡定义。

## 校验契约

保存前在浏览器进行即时校验，接口端再次进行权威校验。有效关卡必须满足以下条件：棋盘为 6×6、8×8、10×10 或 12×12；路标从 1 连续递增；所有路标和解法格位于边界内；解法不重访格子、相邻格仅正交移动、不穿越倒木；解法覆盖全部格子，且依次经过全部路标。

## 接口边界

| 方法 | 路径 | 权限 | 行为 |
|---|---|---|---|
| `GET` | `/api/levels` | 登录 | 按状态、尺寸、难度分页查询管理列表。 |
| `POST` | `/api/levels` | 登录 | 创建逻辑关卡和第一个草稿版本。 |
| `GET` | `/api/levels/:id` | 登录 | 返回关卡、当前版本与历史版本摘要。 |
| `PUT` | `/api/levels/:id/draft` | 登录 | 保存为新的草稿版本并执行校验。 |
| `POST` | `/api/levels/:id/publish` | 登录 | 发布指定的已校验版本。 |
| `POST` | `/api/levels/:id/archive` | 登录 | 归档关卡。 |
| `POST` | `/api/levels/:id/rollback` | 登录 | 将指定历史版本重新发布。 |
| `GET` | `/api/game/levels` | 公开 | 返回已发布关卡的安全游戏载荷，不含 `solution`。 |

## 编辑器体验

编辑器使用“野外手册”构图，但以内容效率优先。左侧是关卡元信息与工具，中央为可点击的林区网格，右侧显示即时校验、版本历史和小游戏载荷预览。编辑工具分为三类：设置路标、切换倒木、编辑/粘贴完整解法。导入导出区域接受本项目的 JSON 格式，方便将现有题库转换为关卡。

> 设计原则：**编辑器允许快速试错，发布接口拒绝无效关卡。**
