# Game Plan: 森林寻径

## Risk Tasks

### 1. 连续林径输入与规则校验

- **Why isolated:** 触摸拖动的采样频率并不稳定；若只依据指针落点，会出现跳格、斜向越过倒木、回退脚印与跳过路标，直接破坏路线谜题的可信度。
- **Approach:** 使用以逻辑格坐标为单位的 `PathEngine`，输入层将鼠标/触摸位置映射为格坐标；仅接受与路径终点正交相邻、可通行、未踏过的下一格。允许回到倒数第二格踩回一步；林间路标必须严格按顺序经过；每次更新后由规则层重新计算完成状态。
- **Verify:** 快速拖动、缓慢拖动与触摸拖动都不会产生斜向连接或跨格跳跃；触到倒木、灌木或已走过的格子不会改变脚印；回到倒数第二格只撤回一步；路标顺序错误被拒绝；走满全部可走林地并抵达最后路标后只触发一次完成。

## Main Build

游戏提供一个原创 6×6 林间谜题，含八枚顺序路标、倒木边界、林间用时、脚印数、踩回一步、重新入林、叶径提示与完成评价。满屏画布绘制林下纸面、路线图、树轮路标、琥珀脚印与通关叶影；React 仅承载画布宿主与 HUD。核心规则继续保持为无渲染依赖的 TypeScript 类，浏览器输入与微信 `wx.onTouch*` 适配仍可共享同一套命令。

- **Assets:**
  - `forestpath-background` — `/manus-storage/forestpath-background_666bf595.png`，覆盖整个游戏视口。
  - `forestpath-visual-target` — `/manus-storage/forestpath-visual-target_547d763c.png`，作为帮助面板艺术参考。
  - `forestpath-logo` — `/manus-storage/forestpath-logo_ee30ae93.png`，作为左上角年轮图标和 favicon 源图。
  - `forestpath-completion-leaves` — `/manus-storage/forestpath-completion-leaves_5dc72282.png`，在完成后叠放于路线图上。
- **Verify:**
  - 指针和触摸坐标都能对应正确格子，路径端点与拖动方向一致。
  - 顶部林地记录、路线图、底部木质工具条在 375px 宽移动屏与桌面屏无溢出或遮挡。
  - 森林背景、图标与完成叶影均可加载，无损坏资源或回退图。
  - `?demo` 自动演示可完整走通，并清晰展示琥珀脚印、路标确认与完成状态。
  - 无浏览器控制台错误；画面符合雨林墨绿、树皮棕、象牙米白与林径琥珀的野外手册气质。

## 交付范围

本次交付为浏览器内可玩的前端原型及微信小游戏迁移说明。由于未提供微信小游戏 AppID 与发布凭证，当前不提交微信开发者工具工程或线上审核包；核心规则与触摸输入边界会按微信 Canvas 的迁移方式组织。
