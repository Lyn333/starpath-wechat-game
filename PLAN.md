# Game Plan: 星轨寻径

## Risk Tasks

### 1. 连续路径输入与规则校验

- **Why isolated:** 触摸拖动的采样频率并不稳定；若只依据指针落点，会出现跳格、斜向误连、回退路径与越墙误判，直接破坏谜题的可信度。
- **Approach:** 使用以逻辑格坐标为单位的 `PathEngine`，输入层将鼠标/触摸位置映射为格坐标；仅接受与路径终点正交相邻、可通行、未占用的下一格。允许拖动回到倒数第二格以撤回一步；检查点必须严格按升序进入；每次更新后由规则层重新计算完成状态。
- **Verify:** 快速拖动、缓慢拖动与触摸拖动都不会产生斜向连接或跨格跳跃；触到墙体或已访问的旧格不改变路径；回到倒数第二格只撤回一格；未按顺序触及检查点被拒绝；走满全部可通行格并抵达最终信标后只触发一次通关。

## Main Build

- 构建单局原创 6×6 航图谜题，含 9 个顺序信标、墙体、计时、步数、撤销、重置、完成评分和帮助说明。
- 使用满屏游戏画布渲染星图背景、网格、墙体、信标、光轨和完成星尘；React 仅作为画布宿主和生命周期外壳。
- 核心规则维护为无渲染依赖的 TypeScript 类；浏览器输入适配与微信 `wx.onTouch*` 适配可共享同一套 `PathEngine` 命令。
- 提供 `?demo` 自动演示：按一条预设的正确路线展示完整通关，用于稳定截图和演示。
- **Assets:**
  - `starpath-archive-background` — `/manus-storage/starpath-archive-background_326814c1.png`，覆盖整个游戏视口。
  - `starpath-visual-target` — `/manus-storage/starpath-visual-target_5f85555d.png`，作为开始态艺术参考与帮助面板底图。
  - `starpath-logo` — `/manus-storage/starpath-logo_88793820.png`，作为左上角品牌星盘和 favicon 源图。
  - `starpath-completion-stars` — `/manus-storage/starpath-completion-stars_d695a03b.png`，在通关时置于完整路线的上方。
- **Verify:**
  - 指针和触摸坐标都能对应正确格子，路径端点与拖动方向一致。
  - 顶部仪器读数、棋盘、底部控制甲板在 375px 宽移动屏与桌面屏都无溢出或遮挡。
  - 已生成背景、图标与星尘素材均可加载，未出现浏览器回退图或损坏图标。
  - 自动演示完整播放时，光轨、检查点确认与通关面板的时序清晰。
  - 无控制台错误；画面符合视觉目标的深靛蓝、黄铜、象牙和青绿色配色、密度与静谧测绘气质。

## 交付范围

本次交付为浏览器内可玩的前端原型及微信小游戏迁移说明。由于未提供微信小游戏 AppID 与发布凭证，当前不提交微信开发者工具工程或线上审核包；核心规则与触摸输入边界会按微信 Canvas 的迁移方式组织。
