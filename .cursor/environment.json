{
  "name": "starpath-wechat-game",
  "install": "COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack enable && COREPACK_ENABLE_DOWNLOAD_PROMPT=0 pnpm install --frozen-lockfile",
  "terminals": [
    {
      "name": "dev",
      "command": "COREPACK_ENABLE_DOWNLOAD_PROMPT=0 pnpm dev",
      "description": "Vite + Express dev server (frontend and tRPC API) on http://localhost:3000"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
